{
  "name": "atividade-crud-produtos",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc && node ./dist",
    "dev": "ts-node-dev --respawn --cls --transpile-only ./src/"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.8",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.2"
  },
  "dependencies": {
    "uuid": "^10.0.0"
  }
}
